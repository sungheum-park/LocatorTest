<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.msync.web.module.scheduler.dao.HistDelMapper">

	<delete id="deleteHistory">
		delete from tb_history where substring(action_time,1,8) &lt;= replace(substring(SUBDATE(NOW(), INTERVAL 18 MONTH),1,10),'-','')
	</delete>

</mapper>
